# API base URL del backend
# Local: apunta al backend Express en desarrollo
NUXT_PUBLIC_API_BASE=http://localhost:3001
# Producción: usa el fallback '/api' que pasa por el Nitro proxy (server/api/[...].ts)
# No requiere variable explícita en Vercel — el proxy resuelve automáticamente
