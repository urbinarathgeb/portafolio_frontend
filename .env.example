# Base de la ruta del formulario de contacto (useContact → ${base}/contacts).
# Por defecto '/api', que usa la server route server/api/contacts.post.ts.
# Temporal: esa ruta reenvía al backend y se reemplaza en A4.
# Local, opcional: apuntar directo al backend Express en desarrollo.
# NUXT_PUBLIC_API_BASE=http://localhost:3001
